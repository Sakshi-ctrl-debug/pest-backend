services:
  - type: web
    name: pest-backend
    env: python
    runtime: python-3.11
    buildCommand: python3.11 -m pip install -r requirements.txt
    startCommand: python3.11 -m uvicorn main:app --host 0.0.0.0 --port 10000